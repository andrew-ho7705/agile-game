import TimerPage from "./TimerPage";

const EstimateScore = () => {
    return (
        <>
            <TimerPage timeInSeconds={10} soundEnabled={false} />
        </>
    );
};

export default EstimateScore;
