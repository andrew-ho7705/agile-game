import TimerPage from "./TimerPage";

const EstimateScore = () => {
    return (
        <>
            <TimerPage timeInSeconds={30} soundEnabled={false} />
        </>
    );
};

export default EstimateScore;
